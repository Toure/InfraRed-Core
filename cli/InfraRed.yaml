---
# InfraRed Configuration file which will contain repo locations
provisioning: 'http://localhost/prov.git'

installer: 'http://localhost/install.git'

tester: 'http://localhost/tester.git'

cleanup: 'http://localhost/cleanup.git'

playbook_path: '/tmp/infrared/repos'

logging:
  log_dir: '/tmp/infrared/logs'
  log_level: 'INFO'
